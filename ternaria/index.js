const userpoints = 500;
const userlevel = userpoints >= 1000 ? 'VIP user' : 'Normal user';

console.log(userlevel);

const usercollor = null;
const pathercollor = usercollor || 'black';

console.log(pathercollor);